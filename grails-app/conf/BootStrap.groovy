import sportstats.Game

class BootStrap {

    def init = { servletContext ->
     /*   if (!Game.count()) {
            def stadium = new Game(homeSeed: 1, homeTeam: 'Lakers', arena: "Stadium").save(failOnError: true)
            def homeTeamCity = new Game(homeTeamCity: "Dc").save(failOnError: true)
            def jimSmith = new Game(homeSeed: 2).save(failOnError: true)
        }*/
    }
                def destroy = {
    }
}
